import styled from 'styled-components';
import { Layout } from 'antd';

export const Canvas = styled(Layout)`
  background: #fff;
  border-radius: 6px;
  box-shadow: 0 2vh 5vw 1vh rgba(0, 0, 0, 0.2);
  width: 90vw;
  height: 80vh;
  margin: 10vh 5vw;
  padding: 2vh;
`;
